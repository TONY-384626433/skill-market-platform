name: Deploy Web (gh-pages)

# 推送 master 时自动构建前端并部署到 gh-pages 分支
# 也可在 Actions 页面手动触发 (workflow_dispatch)
on:
  push:
    branches: [master]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-pages
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci || npm install
          else
            npm install
          fi

      - name: Build
        run: npm run build
        env:
          # 后端地址来自仓库变量 SKILLHUB_API_BASE (隧道地址变了只改这里)
          # 留空则回退同源 /api/v1, 也可在部署后改 gh-pages 上的 config.js
          VITE_API_BASE: ${{ vars.SKILLHUB_API_BASE }}

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/dist
          publish_branch: gh-pages
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'deploy: web 自动构建 (${{ github.sha }})'
